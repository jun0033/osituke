<div class="container m-5 text-left">
  <div class="row">
    <% if @notifications_index.present? %>
    <%= render "notifications", notifications_index: @notifications_index %>
    <% else %>
    <p>通知はありません</p>
    <% end %>
  </div>
</div>