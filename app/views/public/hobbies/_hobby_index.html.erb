<table>
  <tr>
    <% hobbies.each do |hobby| %>
    <td>
      <%= link_to user_path(hobby.user_id) do %>
      <%= image_tag hobby.user.get_profile_image(80,80)%>
      <% end %>
    </td>
    <td>
      <%= link_to "#{hobby.title}", hobby_path(hobby.id) %>
    </td>
    <td>
      <%= hobby.body %>
    </td>
    <td><%= link_to hobby_path(hobby.id) do %>
    <%= book.book_comments.count %> コメント
    <% end %></td>
  </tr>
    <% end %>
</table>